import { FilterProvider } from "./filter.provider";
import { FilterContext } from "./filter.context";
import {
  categoryFilter,
  sortByPrice,
  sortBySize,
  getAllProducts,
} from "./filter.actions";

export {
  FilterContext,
  FilterProvider,
  categoryFilter,
  sortByPrice,
  sortBySize,
  getAllProducts,
};
