import { FilterProvider } from "./filter.provider";
import { FilterContext } from "./filter.context";
import {
  sortByCategory,
  sortByPrice,
  sortBySize,
  getAllProducts,
} from "./filter.actions";

export {
  FilterContext,
  FilterProvider,
  sortByCategory,
  sortByPrice,
  sortBySize,
  getAllProducts,
};
