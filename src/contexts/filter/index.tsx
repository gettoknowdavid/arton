import { FilterProvider } from "./filter.provider";
import { FilterContext } from "./filter.context";
import {
  categoryFilter,
  priceFilter,
  sortBySize,
  getAllProducts,
} from "./filter.actions";

export {
  FilterContext,
  FilterProvider,
  categoryFilter,
  priceFilter,
  sortBySize,
  getAllProducts,
};
